= render 'shared/resources/header', title: t('.title'), policy: policy(@contest.photos.build)

= render 'shared/resources/list_as_table', attributes: user_index_attributes(%i(title nomination competitor), Photo), collection: @photos

/.row
/  - @photos.each do |photo|
/    .col-12.col-sm-6.col-md-4.col-lg-3
/      .card.mr-2.mb-2
/        = link_to photo do
/          img.card-img-top.img-fluid src=(photo.image_url(:medium)) alt=photo.title style="max-height: 14em"
/        .card-block
/          = link_to photo do
/            h6.card-title = photo.title
/          .d-flex.flex-nowrap.justify-content-start.align-items-center
/            = render 'shared/resources/actions', path: { id: photo }, hide: [:show]
