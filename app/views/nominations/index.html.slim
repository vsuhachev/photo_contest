= render 'shared/resources/header', title: t('.title'), policy: policy(@contest.nominations.build)

= render 'shared/resources/list_as_table', attributes: %i(title description), collection: @nominations
