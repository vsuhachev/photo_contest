= render 'shared/resources/header', title: t('.title'), policy: policy(@contest.competitors.build)

= render 'shared/resources/list_as_table', attributes: user_index_attributes(%i(fio location organization), Competitor), collection: @competitors
